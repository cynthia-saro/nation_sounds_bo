{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <a href="{{ path('artists') }} ">Retour</a>
    <h1>Nom de l'artiste : {{artist.name}}</h1>
    <img src="{{artist.picture}}">
    <div>Genre : {{artist.category}}</div>
    <div class="descriptionArtist">{{artist.description}}</div>
{% endblock %}
